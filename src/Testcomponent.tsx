
const TestComponent = ({count } : {count: number})=> {
    return (
<div>
Still knocking some rust off, but heres the count value passed from the main app component. {count}
</div>
    )
}
export default TestComponent;
